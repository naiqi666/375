name: Update Docs

on:
  workflow_call:

jobs:
  update-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Update documentation
        if: github.ref == 'refs/heads/main'
        run: npm run autodoc